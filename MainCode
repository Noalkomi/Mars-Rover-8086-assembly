data segment
    uniid db "20230632"  
    uniid2 db "20258275$"
    row db 6 dup(?)  
    col db 6 dup(?)
    grid db 64 dup('.')
ends

stack segment
    dw 128 dup(0)
ends

code segment
start:
    mov ax, data
    mov ds, ax

    lea si, uniid
    add si, 7
    lea di, col
    call obst

    lea si, uniid
    add si, 6
    lea di, row
    call obst

    lea si, uniid2
    add si, 7
    lea di, col 
    add di,3 
    call obst

    lea si, uniid2
    add si, 6
    lea di, row 
    add di,3 
    call obst
    
    
    mov cx, 6           
    xor bx, bx    
         
  gen_craters:
    push cx
    lea si, row
    mov al, [si+bx]
    xor ah, ah
    mov cl, 8
    mul cl
    lea si, col
    add al, [si+bx]
    xor ah, ah
    mov dx, ax
    lea di, grid
    add di, dx
    mov ax, ds
    mov es, ax
    mov es:[di], 'O'
    inc bx
    pop cx
    loop gen_craters
    lea si, grid
    mov bx, 0
find_r: cmp [si+bx], 'O'
    je shift
    mov [si+bx], 'R'
    jmp done
shift: inc bx
    jmp find_r
done: call dispgri
    mov ax, 4C00h
    int 21h
obst proc
    mov cx, 3
l1: 
    mov al, [si]
    sub al, 30H
    xor ah, ah
    mov bl, 8
    div bl
    mov [di], ah
    sub si, 2
    add di, 1
    loop l1
    ret
obst endp


dispgri proc
    mov dl, ' '
    mov ah, 02h
    int 21h
    mov dl, ' '
    int 21h
    mov bx, 0
print_col:
    mov dl, bl
    add dl, 30h
    mov ah, 02h
    int 21h
    mov dl, ' '
    int 21h
    inc bx
    cmp bx, 8
    jl print_col
    mov dl, 13
    mov ah, 02h
    int 21h
    mov dl, 10
    int 21h
    lea si, grid
    mov bx, 0
lo1:
    mov dl, bl
    add dl, 30h
    mov ah, 02h
    int 21h
    mov dl, ' '
    int 21h
    mov cx, 8
lo2:
    mov dl, [si]
    mov ah, 02h
    int 21h
    mov dl, ' '
    int 21h
    inc si
    loop lo2
    mov dl, 13
    mov ah, 02h
    int 21h
    mov dl, 10
    int 21h
    inc bx
    cmp bx, 8
    jl lo1
    ret
dispgri endp
  
  
ends
end start
